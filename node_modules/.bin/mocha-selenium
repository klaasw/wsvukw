#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../mocha-selenium/bin/mocha-selenium.js" "$@"
  ret=$?
else 
  node  "$basedir/../mocha-selenium/bin/mocha-selenium.js" "$@"
  ret=$?
fi
exit $ret
